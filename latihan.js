function iterateArray(array) {
  for (let i = 0; i < array.length; i++) {
    const item = array[i];
    // Perform desired action for each item in the array
    console.log(item); // Example: Output each item to the console
    // Other code...
  }
}

// Example usage:
const myArray = [1, 2, 3, 4, 5];
console.log(iterateArray(myArray));
